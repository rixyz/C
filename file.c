//WAP to insert 3 num and add
#include <stdio.h>
void main()
{
   int var1, var2, var3, sum;
   printf("Enter 3 numbers\n");
   scanf("%d%d%d", &var1, &var2, &var3);
   sum = var1 + var2 + var3;
   printf("Result is %d", var1&var2);
}

//WAP to take feignheit as input from user and convert it onto Celcius
//WAP to tkae input of 3 sides of a triangle as input and calculate area and display it
